@using LibraryApp.Models.LoanViewModels
@model CreateViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>Loan</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <!--<input type="hidden" asp-for="RFID" />-->
        <!--<input type="hidden" asp-for="Code" />-->
        <div class="form-group">
            <label asp-for="ReturnDate" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="ReturnDate" class="form-control" />
                <span asp-validation-for="ReturnDate" class="text-danger"></span>
            </div>
        </div>
       <div class="form-group">
            <label asp-for="isReturned" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="isReturned" class="form-control" />
                <span asp-validation-for="isReturned" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}